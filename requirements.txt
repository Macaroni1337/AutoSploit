# AutoSploit Python 3 requirements
# Migrated from Python 2.7 — all packages verified compatible with Python 3.10+
requests==2.32.3
psutil==6.1.1
beautifulsoup4==4.12.3

# Official Shodan Python library — replaces the previous raw-HTTP Shodan client
shodan==1.31.0

# pymetasploit3 — available for RPC-based Metasploit interaction.
# The current exploiter uses subprocess/RC-scripts (no daemon required).
# To use the RPC client instead, start msfrpcd and see lib/exploitation/exploiter.py.
pymetasploit3==1.0.3
